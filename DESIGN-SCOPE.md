Allowed:
- /assets/style.css
- HTML structure + Tailwind classes in pages
- /images and /assets/images (visuals only)

Not allowed:
- Any JS modules (offers.js, chat.js, header.js, etc.)
- JSON plan data
- Backend routes